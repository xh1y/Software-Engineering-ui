# UI 模块测试

## 1. 主窗口模块测试（MainWindow）

### 1.1 初始化与布局测试

| 用例编号 | 测试项 | 操作步骤 / 输入条件 | 预期结果 | 实际结果 |
| -------- | ------ | ------------------- | -------- | -------- |
| MW-01 | 窗口初始化 | 创建 `MainWindow` 实例 | 标题为 "OptiKG - 工业知识抽取系统"，尺寸 1200×800 | 通过 |
| MW-02 | 分割器布局 | 启动后观察布局 | 左侧：抽取面板+历史记录；右侧：图谱+结果树 | 通过 |
| MW-03 | 数据库初始化 | 启动程序 | `DatabaseManager::initialize()` 被调用，失败时显示 toast | 通过 |
| MW-04 | 历史记录加载 | 启动程序 | `historyPanel_->loadHistory()` 被调用 | 通过 |
| MW-05 | 设置加载 | 启动程序 | `ConfigManager::initialize()` 和 `loadSettings()` 被调用 | 通过 |
| MW-06 | 关闭事件 | 点击窗口关闭按钮 | `saveSettings()` 被调用，事件被接受 | 通过 |

### 1.2 菜单栏与工具栏测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| MW-07 | 文件菜单 - 新建 | 文件→新建 (Ctrl+N) | 触发 `onClearClicked()` | 通过 |
| MW-08 | 文件菜单 - 打开结果 | 文件→打开之前的结果 (Ctrl+O) | 触发 `onOpenClicked()` | 通过 |
| MW-09 | 文件菜单 - 打开文本 | 文件→打开纯文本文件 (Ctrl+Shift+O) | 触发 `onOpenTextFileClicked()` | 通过 |
| MW-10 | 文件菜单 - 保存 | 文件→保存 (Ctrl+S) | 触发 `onSaveClicked()` | 通过 |
| MW-11 | 文件菜单 - 导出 JSON | 文件→导出为JSON | 触发 `onExportJsonClicked()` | 通过 |
| MW-12 | 文件菜单 - 导出 CSV | 文件→导出为CSV | 触发 `onExportCsvClicked()` | 通过 |
| MW-13 | 编辑菜单 - 抽取 | 编辑→抽取 (Ctrl+E) | 触发 `onExtractClicked()` | 通过 |
| MW-14 | 编辑菜单 - 清空 | 编辑→清空 (Ctrl+Shift+C) | 触发 `onClearClicked()` | 通过 |
| MW-15 | 视图菜单 - 放大 | 视图→放大 (Ctrl++) | `graphWidget_->zoomIn()` 被调用 | 通过 |
| MW-16 | 视图菜单 - 缩小 | 视图→缩小 (Ctrl+-) | `graphWidget_->zoomOut()` 被调用 | 通过 |
| MW-17 | 视图菜单 - 适应视图 | 视图→适应视图 (Ctrl+0) | `graphWidget_->fitToView()` 被调用 | 通过 |
| MW-18 | 视图菜单 - 重置缩放 | 视图→重置缩放 (Ctrl+R) | `graphWidget_->resetZoom()` 被调用 | 通过 |
| MW-19 | 视图菜单 - 面板可见性切换 | 视图→显示知识图谱/结果列表/历史记录 | 对应面板 setVisible 切换 | 通过 |
| MW-20 | 视图菜单 - 重置布局 | 视图→重置布局 (Ctrl+Shift+R) | 所有分割器恢复均等比例 | 通过 |
| MW-21 | 工具菜单 - 批量处理 | 工具→批量处理 (Ctrl+B) | 打开 `BatchProcessDialog` | 通过 |
| MW-22 | 工具菜单 - 设置 | 工具→设置 (Ctrl+,) | 打开 `SettingsDialog` | 通过 |
| MW-23 | 帮助菜单 - 使用说明 | 帮助→使用说明 (F1) | 触发 `onHelpClicked()` | 通过 |
| MW-24 | 帮助菜单 - 关于 | 帮助→关于 | 触发 `onAboutClicked()` | 通过 |
| MW-25 | 工具栏按钮 - 抽取 | 点击工具栏"抽取"按钮 | 触发 `onExtractClicked()` | 通过 |
| MW-26 | 工具栏按钮 - 清空 | 点击工具栏"清空"按钮 | 触发 `onClearClicked()` | 通过 |

### 1.3 抽取流程测试

| 用例编号 | 测试项 | 操作步骤 / 输入条件 | 预期结果 | 实际结果 |
| -------- | ------ | ------------------- | -------- | -------- |
| MW-27 | 空文本抽取 | 输入框为空，点击抽取 | 状态栏显示 "请输入文本内容" | 通过 |
| MW-28 | 正常文本抽取 | 输入有效文本，点击抽取 | 创建 `InferenceWorker`，启动线程 | 通过 |
| MW-29 | 重复抽取保护 | 当前线程运行中再次点击抽取 | 先停止旧线程，再启动新线程 | 通过 |
| MW-30 | 模型路径自动检测 | `config.getModelPath()` 为空 | 调用 `autoDetectModelPath()` | 通过 |
| MW-31 | 阈值传递 | `config.getThreshold()` = -5.0 | 传递给 `InferenceWorker` | 通过 |
| MW-32 | 抽取开始状态 | 点击抽取后 | 进度条显示，状态栏 "正在抽取..."，抽取面板禁用 | 通过 |
| MW-33 | 抽取完成处理 | 线程返回结果 | 保存数据库，更新结果树，更新图谱，恢复面板 | 通过 |
| MW-34 | 抽取结果保存数据库 | 返回非空结果 | `DatabaseManager::insertExtractionRecord` 被调用 | 通过 |
| MW-35 | 抽取空结果不保存 | 返回空结果 | 不调用 insertExtractionRecord | 通过 |
| MW-36 | 抽取失败处理 | 线程发射 errorOccurred | 隐藏进度条，恢复面板，显示错误 toast | 通过 |
| MW-37 | 进度更新 | 线程发射 progressChanged | `progressBar_->setValue()` 更新 | 通过 |

### 1.4 结果展示与交互测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| MW-38 | 结果树到图谱高亮 - 实体 | 点击结果树中的实体项 | `graphWidget_->highlightNode(name)` 被调用 | 通过 |
| MW-39 | 结果树到图谱高亮 - 关系 | 双击结果树中的三元组项 | `graphWidget_->highlightPath(s, t)` 被调用 | 通过 |
| MW-40 | 图谱节点点击信号 | （图谱内部实现） | `nodeClicked` 信号可被外部接收 | 通过 |
| MW-41 | 状态栏计数更新 | 抽取完成，3 个 triples | 显示 "实体: 6 关系: 3" | 通过 |
| MW-42 | Toast 成功显示 | `showToast("成功", false)` | 状态栏绿色高亮，3 秒后恢复 | 通过 |
| MW-43 | Toast 错误显示 | `showToast("错误", true)` | 状态栏红色高亮，3 秒后恢复 | 通过 |

### 1.5 数据操作测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| MW-44 | 清空操作 | 点击清空 | 输入框清空，结果树清空，图谱清空，计数归零 | 通过 |
| MW-45 | 导出 JSON - 有数据 | 当前有结果，文件→导出 JSON | 弹出保存对话框，生成正确格式文件 | 通过 |
| MW-46 | 导出 JSON - 无数据 | 当前无结果 | 状态栏提示 "没有可导出的数据" | 通过 |
| MW-47 | 导出 CSV - 有数据 | 当前有结果，文件→导出 CSV | 弹出保存对话框，生成正确格式文件 | 通过 |
| MW-48 | 打开 JSON 文件 | 文件→打开之前的结果 | 解析 triples，更新结果树和图谱 | 通过 |
| MW-49 | 打开批量结果格式 | 打开含 "results" 数组的 JSON | 合并所有 triples 并显示 | 通过 |
| MW-50 | 打开无效 JSON | 选择非 JSON 文件 | 显示解析错误 toast | 通过 |
| MW-51 | 批量删除历史记录 | HistoryPanel 发射 batchDeleteClicked | `DatabaseManager::deleteMultipleRecords` 被调用 | 通过 |
| MW-52 | 数据库变化刷新 | `DatabaseManager::databaseChanged` 信号发射 | `historyPanel_->loadHistory()` 被调用 | 通过 |

---

## 2. 文本抽取面板测试（ExtractionPanel）

| 用例编号 | 测试项 | 操作步骤 / 输入条件 | 预期结果 | 实际结果 |
| -------- | ------ | ------------------- | -------- | -------- |
| EP-01 | 初始化布局 | 创建 `ExtractionPanel` | 包含标题、文本编辑框、字符计数、两个按钮 | 通过 |
| EP-02 | 占位符文本 | 首次显示 | 文本框显示 "请输入发动机异响、液压系统故障等维修描述文本..." | 通过 |
| EP-03 | 字符计数初始值 | 空文本 | 显示 "字符数: 0/5000" | 通过 |
| EP-04 | 输入文本计数 | 输入 "测试文本"（4 字符） | 显示 "字符数: 4/5000" | 通过 |
| EP-05 | 最大字符限制 | 输入 5001 字符 | 自动截断为 5000 字符，光标在末尾 | 通过 |
| EP-06 | 字符数正常颜色 | 字符数 < 4500 | 颜色为 #666666 | 通过 |
| EP-07 | 字符数警告颜色 | 字符数 4501~4999 | 颜色为 #FF9800 | 通过 |
| EP-08 | 字符数错误颜色 | 字符数 = 5000 | 颜色为 #f44336 | 通过 |
| EP-09 | 抽取按钮信号 | 点击"开始抽取" | 发射 `extractClicked()` 信号 | 通过 |
| EP-10 | 清空按钮信号 | 点击"清空" | 发射 `clearClicked()` 信号，文本清空 | 通过 |
| EP-11 | 文本改变信号 | 在文本框中输入 | 发射 `textChanged(QString)` 信号 | 通过 |
| EP-12 | setText 接口 | `setText("测试")` | 文本框更新，字符计数更新 | 通过 |
| EP-13 | text() 接口 | 文本框含 " 测试 " | 返回 "测试"（trimmed） | 通过 |
| EP-14 | setEnabled(false) | 抽取过程中 | 文本框和按钮均禁用 | 通过 |
| EP-15 | setEnabled(true) | 抽取完成后 | 文本框和按钮均启用 | 通过 |

---

## 3. 知识图谱可视化测试（GraphWidget）

### 3.1 初始化与空状态测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| GW-01 | 初始化 | 创建 `GraphWidget` | 场景创建，背景色 #F5F5F5，抗锯齿开启 | 通过 |
| GW-02 | 空状态显示 | 无数据时 | 显示网络图标和 "暂无知识图谱数据" 提示 | 通过 |
| GW-03 | 空状态副标题 | 无数据时 | 显示 "在左侧输入文本，点击「开始抽取」生成知识图谱" | 通过 |
| GW-04 | 空状态不拦截事件 | 无数据时点击空白处 | 可以正常触发背景拖动 | 通过 |

### 3.2 数据加载与布局测试

| 用例编号 | 测试项 | 输入条件 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| GW-05 | 加载空数据 | `setGraphData({}, {})` | 显示空状态，调用 `clearGraph()` | 通过 |
| GW-06 | 加载单节点 | 1 个 node，0 条 edge | 隐藏空状态，节点居中显示 | 通过 |
| GW-07 | 加载多节点 | 20 个 nodes，30 条 edges | 隐藏空状态，创建所有节点和边，执行力导向布局 | 通过 |
| GW-08 | 节点颜色正确性 | 部件/故障/工具/组成 | 分别显示对应马卡龙色系 | 通过 |
| GW-09 | 节点大小自适应 | 节点名称长度不同 | 椭圆宽度在 [80, 200] 之间动态调整 | 通过 |
| GW-10 | 文本截断 | 节点名称很长 | 超过椭圆宽度 - 20 时显示省略号 | 通过 |
| GW-11 | 力导向布局参数 | 3 个节点 | 斥力 200，引力 0.01，阻尼 0.8，80 次迭代 | 通过 |
| GW-12 | 力导向布局参数 | 50 个节点 | 斥力 100，引力 0.035，阻尼 0.9，120 次迭代 | 通过 |
| GW-13 | 全局重力收敛 | 任意数量节点 | 所有节点向中心 (0,0) 收敛 | 通过 |
| GW-14 | 加载后适应视图 | 新数据加载完成 | 自动调用 `fitToView()` | 通过 |

### 3.3 边渲染测试

| 用例编号 | 测试项 | 输入条件 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| GW-15 | 高置信度边样式 | confidence > 0.8 | 绿色粗线 (2.5px) | 通过 |
| GW-16 | 中置信度边样式 | 0.5 < confidence ≤ 0.8 | 橙色线 (2px) | 通过 |
| GW-17 | 低置信度边样式 | confidence ≤ 0.5 | 灰色线 (1.5px) | 通过 |
| GW-18 | 边曲线控制 | 存在多条平行边 | 根据哈希值计算不同偏移方向，减少交叉 | 通过 |
| GW-19 | 关系文本位置 | 任意边 | 文本位于二次贝塞尔曲线中点 | 通过 |

### 3.4 交互操作测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| GW-20 | 背景拖动 | 在空白处按住左键移动 | 画布跟随鼠标平移 | 通过 |
| GW-21 | 节点拖拽 | 鼠标拖拽节点 | 节点移动，所有连接边实时更新 | 通过 |
| GW-22 | 滚轮缩放 | 滚轮滚动（无 Ctrl） | 垂直滚动（默认行为） | 通过 |
| GW-23 | Ctrl+滚轮放大 | Ctrl+滚轮向上 | `zoomIn()`，scale 增加 1.2 倍 | 通过 |
| GW-24 | Ctrl+滚轮缩小 | Ctrl+滚轮向下 | `zoomOut()`，scale 减少 1.2 倍 | 通过 |
| GW-25 | 重置缩放 | 调用 `resetZoom()` | 变换矩阵恢复单位矩阵 | 通过 |
| GW-26 | 适应视图 | 调用 `fitToView()` | 所有节点和边可见并居中 | 通过 |
| GW-27 | 适应视图 - 空数据 | 无数据时 | 空状态组居中显示 | 通过 |
| GW-28 | 节点高亮 | `highlightNode("轴承")` | 指定节点变为柔和黄色高亮 | 通过 |
| GW-29 | 路径高亮 | `highlightPath("轴承", "磨损")` | 源节点浅红、目标节点浅绿、边变蓝色粗线 | 通过 |
| GW-30 | 高亮重置 | 再次高亮其他节点 | 之前高亮恢复默认颜色 | 通过 |
| GW-31 | 图例显示 | 有数据时 | 右下角显示 "图例" 及 4 种实体类型色块 | 通过 |
| GW-32 | 清除图谱 | `clearGraph()` | 删除所有节点和边，显示空状态 | 通过 |
| GW-33 | 窗口大小改变 | 调整 widget 大小 | 调用 `centerOn(0,0)` 保持居中 | 通过 |

---

## 4. 历史记录面板测试（HistoryPanel）

### 4.1 初始化与表格测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| HP-01 | 初始化布局 | 创建 `HistoryPanel` | 包含标题、筛选区、表格、操作按钮 | 通过 |
| HP-02 | 表格列头 | 首次显示 | 6 列：ID、时间、处理时间(ms)、平均置信度、实体数、关系数 | 通过 |
| HP-03 | 表格样式 | 首次显示 | 无网格线、无交替行色、无垂直表头、行高 40px | 通过 |
| HP-04 | 加载历史记录 | 启动或点击刷新 | 从数据库查询并填充表格 | 通过 |
| HP-05 | 无数据显示 | 数据库为空 | 表格显示 "未找到匹配的记录"，合并所有列 | 通过 |
| HP-06 | 有数据显示 | 数据库有 5 条记录 | 显示 5 行数据，ID 降序或时间降序 | 通过 |

### 4.2 筛选与搜索测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| HP-07 | 关键词搜索 | 输入 "电机"，点击搜索 | 调用 `searchExtractionRecords("电机", ...)` | 通过 |
| HP-08 | 日期范围筛选 | 选择 2024-01-01 至 2024-12-31，点击搜索 | 带起止时间的 QDateTime 传递给搜索 | 通过 |
| HP-09 | 实体类型筛选 | 选择"部件"，点击搜索 | 传递 entityType=0 进行搜索 | 通过 |
| HP-10 | 组合筛选 | 同时输入关键词、日期、类型 | 三者条件同时生效 | 通过 |
| HP-11 | 清空筛选 | 点击"清空筛选" | 所有条件重置为默认值，重新加载全部历史 | 通过 |
| HP-12 | 搜索无结果 | 输入不存在的关键词 | 表格显示 "未找到匹配的记录" | 通过 |

### 4.3 交互操作测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| HP-13 | 单击记录 | 单击表格某行 | 发射 `recordClicked(id)` 信号 | 通过 |
| HP-14 | 双击记录 | 双击表格某行 | 发射 `recordDoubleClicked(id)` 信号 | 通过 |
| HP-15 | 多选记录 | Ctrl/Shift + 点击多行 | `selectedRecordIds()` 返回多个 ID | 通过 |
| HP-16 | 导出选中 - 未选择 | 未选中任何行，点击"导出选中" | QMessageBox 警告 "请先选择要导出的记录" | 通过 |
| HP-17 | 导出选中 - 已选择 | 选中 2 行，点击"导出选中" | 发射 `exportClicked(ids)` 信号 | 通过 |
| HP-18 | 批量删除 - 未选择 | 未选中任何行，点击"批量删除" | QMessageBox 警告 "请先选择要删除的记录" | 通过 |
| HP-19 | 批量删除 - 确认 | 选中 2 行，点击"批量删除" | 弹出确认框，确认后发射 `batchDeleteClicked(ids)` | 通过 |
| HP-20 | 批量删除 - 取消 | 选中 2 行，点击"批量删除"后选"否" | 不发射删除信号 | 通过 |
| HP-21 | 刷新按钮 | 点击"刷新" | 调用 `loadHistory()`，表格数据更新 | 通过 |
| HP-22 | 删除后自动刷新 | `removeRecord(id)` 成功 | 表格自动重新加载 | 通过 |

---

## 5. 批量处理对话框测试（BatchProcessDialog）

### 5.1 初始化与文件列表测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| BPD-01 | 初始化加载配置 | 打开对话框 | autoExport、saveToDb、exportFormat 从配置加载 | 通过 |
| BPD-02 | 添加文件 | 点击"添加文件..." | 弹出 QFileDialog，选中文件后添加到列表 | 通过 |
| BPD-03 | 添加重复文件 | 添加已存在的文件 | 列表中不重复添加 | 通过 |
| BPD-04 | 添加目录 | 点击"添加目录..." | 递归添加目录下所有 JSON/CSV 文件 | 通过 |
| BPD-05 | 添加空目录 | 选择无 JSON/CSV 的目录 | 弹出信息提示 "所选目录中没有找到 JSON 或 CSV 文件" | 通过 |
| BPD-06 | 移除选中文件 | 选中文件，点击"移除选中" | 从列表中删除选中项 | 通过 |
| BPD-07 | 清空列表 | 点击"清空列表" | 列表清空，开始按钮禁用 | 通过 |
| BPD-08 | 文件选择状态联动 | 选中/取消选中文件 | "移除选中"按钮状态联动 | 通过 |

### 5.2 处理流程测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| BPD-09 | 无文件开始处理 | 列表为空，点击"开始处理" | QMessageBox 警告 "请先添加要处理的文件" | 通过 |
| BPD-10 | 正常开始处理 | 列表有文件，点击"开始处理" | 状态重置，`BatchProcessor` 启动，日志显示开始信息 | 通过 |
| BPD-11 | 处理中按钮状态 | 处理开始后 | 添加/移除/清空/开始/浏览按钮禁用，停止按钮启用 | 通过 |
| BPD-12 | 扫描进度显示 | 处理开始后 | 状态栏显示 "正在扫描文件 (x/y): 文件名" | 通过 |
| BPD-13 | 扫描完成后进度条 | 扫描结束 | 进度条范围设为 0~总记录数 | 通过 |
| BPD-14 | 处理进度显示 | 处理过程中 | 进度条和状态标签实时更新当前文件 | 通过 |
| BPD-15 | 文件处理成功日志 | 单个文件处理成功 | 日志显示 ✓ 文件名、记录数、三元组数、耗时 | 通过 |
| BPD-16 | 零三元组提示 | 处理成功但无三元组 | 日志显示原因分析（阈值/模型路径/内容不匹配） | 通过 |
| BPD-17 | 文件处理失败日志 | 单个文件处理失败 | 日志显示 ✗ 文件名、错误原因 | 通过 |
| BPD-18 | 处理统计更新 | 过程中 | 统计标签显示 "文件: x \| 成功: y \| 失败: z \| 三元组: w" | 通过 |
| BPD-19 | 停止处理 | 点击"停止" | `processor_->stop()` 被调用，状态显示 "正在停止..." | 通过 |
| BPD-20 | 处理完成提示 | 所有文件处理完毕 | QMessageBox 显示完成摘要 | 通过 |

### 5.3 导出与保存测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| BPD-21 | 自动导出 JSON | 勾选自动导出，处理成功 | 按时间戳生成 JSON 结果文件 | 通过 |
| BPD-22 | 自动导出 CSV | 格式选 CSV，勾选自动导出 | 生成 CSV 结果文件 | 通过 |
| BPD-23 | 指定输出路径 | 填写输出路径 | 结果导出到指定路径 | 通过 |
| BPD-24 | 保存到数据库 | 勾选保存到数据库 | 调用 `saveToDatabase()`，成功则日志提示 | 通过 |
| BPD-25 | 导出结果按钮 | 处理完成后点击"导出结果..." | 弹出保存对话框并导出 | 通过 |
| BPD-26 | 无成功结果导出 | 全部失败，点击"导出结果..." | 生成空文件或跳过（当前实现导出空结果） | 通过 |

### 5.4 关闭行为测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| BPD-27 | 空闲时关闭 | 未处理中，点击关闭 | 直接关闭对话框 | 通过 |
| BPD-28 | 处理中关闭 - 确认 | 处理中点击关闭，选"是" | 停止处理，关闭对话框 | 通过 |
| BPD-29 | 处理中关闭 - 取消 | 处理中点击关闭，选"否" | 继续处理，不关闭 | 通过 |

---

## 6. 结果树控件测试（ResultTreeWidget）

| 用例编号 | 测试项 | 输入条件 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| RTW-01 | 初始化 | 创建 `ResultTreeWidget` | 3 列：名称、类型、置信度；按置信度降序 | 通过 |
| RTW-02 | 空结果提示 | `setResults({})` | 显示 "暂无抽取结果"，灰色居中，不可选中 | 通过 |
| RTW-03 | 实体分组显示 | 2 个 triples，subject 类型为 Component | 生成 "部件 (2)" 分组，展开状态 | 通过 |
| RTW-04 | 实体去重 | 同一实体名出现多次 | 分组内只显示一次 | 通过 |
| RTW-05 | 关系分组显示 | 2 个 triples，relation="相关" | 生成 "相关 (2)" 分组 | 通过 |
| RTW-06 | 置信度颜色 - 高 | confidence = 0.85 | 第 3 列显示绿色 (0, 150, 0) | 通过 |
| RTW-07 | 置信度颜色 - 中 | confidence = 0.7 | 第 3 列显示橙色 (255, 140, 0) | 通过 |
| RTW-08 | 置信度颜色 - 低 | confidence = 0.4 | 第 3 列显示红色 (220, 0, 0) | 通过 |
| RTW-09 | 置信度格式化 | confidence = 0.8567 | 显示 "0.86"（保留两位小数） | 通过 |
| RTW-10 | 实体项点击信号 | 点击实体子项 | 发射 `entityClicked(name, type)` | 通过 |
| RTW-11 | 三元组项点击信号 | 点击三元组子项 | 发射 `tripleClicked(triple)` | 通过 |
| RTW-12 | 清空结果 | `clearResults()` | 树清空 | 通过 |
| RTW-13 | 排序功能 | 多行数据 | 默认按第 3 列置信度降序排序 | 通过 |

---

## 7. 设置对话框测试（SettingsDialog）

### 7.1 初始化与加载测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| SD-01 | 初始化布局 | 打开设置对话框 | 3 个标签页：字段映射、模型设置、批量处理 | 通过 |
| SD-02 | 加载字段映射 | 打开对话框 | JSON 字段、CSV 列名、编码从配置加载 | 通过 |
| SD-03 | 加载模型设置 | 打开对话框 | 模型路径、阈值从配置加载 | 通过 |
| SD-04 | 加载批量设置 | 打开对话框 | 输出目录、自动导出、格式从配置加载 | 通过 |
| SD-05 | 编码下拉框匹配 | 配置编码为 "GBK" | 下拉框选中 GBK | 通过 |
| SD-06 | 编码下拉框自定义 | 配置编码不在列表中 | 下拉框显示自定义文本 | 通过 |

### 7.2 操作与保存测试

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 实际结果 |
| -------- | ------ | -------- | -------- | -------- |
| SD-07 | 应用设置 | 修改值后点击"应用" | 保存到 ConfigManager，发出提示音 | 通过 |
| SD-08 | 确定保存 | 修改值后点击"确定" | 保存配置并关闭对话框 | 通过 |
| SD-09 | 取消无修改 | 未修改值，点击"取消" | 直接关闭对话框 | 通过 |
| SD-10 | 取消有修改 - 确认 | 修改值后点击"取消"，选"是" | 不保存，关闭对话框 | 通过 |
| SD-11 | 取消有修改 - 保留 | 修改值后点击"取消"，选"否" | 回到对话框继续编辑 | 通过 |
| SD-12 | 恢复默认 - 确认 | 点击"恢复默认"，选"是" | 所有字段恢复为 AppConstants 默认值 | 通过 |
| SD-13 | 恢复默认 - 取消 | 点击"恢复默认"，选"否" | 保持当前值不变 | 通过 |
| SD-14 | 浏览模型文件 | 点击"浏览..."（模型页） | 弹出文件选择对话框，过滤 *.onnx | 通过 |
| SD-15 | 浏览输出目录 | 点击"浏览..."（批量页） | 弹出目录选择对话框 | 通过 |
| SD-16 | 阈值范围限制 | 尝试输入 1.0 | QDoubleSpinBox 限制为 [THRESHOLD_MIN, THRESHOLD_MAX] | 通过 |
| SD-17 | 阈值步进 | 点击上下箭头 | 按 THRESHOLD_STEP(1.0) 增减 | 通过 |
| SD-18 | 值变化标记 | 修改任意输入框内容 | `hasChanges_` 标记为 true | 通过 |
