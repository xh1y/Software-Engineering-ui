SourceURL:file:///home/arch/Downloads/软件工程大作业.docx

# 第5章 编码实现

## 5.1 开发环境与工具

| 项目         | 说明                      |
| ------------ | ------------------------- |
| 操作系统     | Windows 11                |
| 集成开发环境 | Clion                     |
| 编程语言     | C++17                     |
| GUI框架      | Qt 6.11                   |
| 深度学习推理 | ONNX Runtime 1.24         |
| 数据库       | SQLite 3                  |
| 版本控制     | Git，托管于Github私有仓库 |

## 5.2 核心模块实现

### 5.2.1 系统核心类图

```mermaid
classDiagram
    direction TB
    
    class MainWindow {
        -ExtractionPanel* extractionPanel_
        -GraphWidget* graphWidget_
        -HistoryPanel* historyPanel_
        -ResultTreeWidget* resultTree_
        -InferenceWorker* currentWorker_
        -QList~Triple~ currentResults_
        +setupUi()
        +setupMenuBar()
        +setupToolBar()
        +setupStatusBar()
        +onExtractClicked()
        +onExtractionFinished()
        +onExportJsonClicked()
        +onBatchProcessClicked()
    }
    
    class ExtractionPanel {
        -QTextEdit* textEdit_
        -QPushButton* extractBtn_
        -QPushButton* clearBtn_
        -QLabel* charCountLabel_
        -MAX_CHARS: 5000
        +text() QString
        +setText()
        +clear()
        +setEnabled()
        +extractClicked() Signal
        +clearClicked() Signal
    }
    
    class HistoryPanel {
        -QTableWidget* tableWidget_
        -QLineEdit* searchEdit_
        -QComboBox* entityTypeFilter_
        -QDateEdit* startDateFilter_
        -QDateEdit* endDateFilter_
        +loadHistory()
        +addRecord()
        +removeRecord()
        +selectedRecordIds() QList~qint64~
        +recordClicked() Signal
        +exportClicked() Signal
        +batchDeleteClicked() Signal
    }
    
    class GraphWidget {
        -QGraphicsScene* scene_
        -QHash~QString,GraphNode*~ nodes_
        -QList~GraphEdge*~ edges_
        -qreal scaleFactor_
        +setGraphData()
        +clearGraph()
        +highlightNode()
        +highlightPath()
        +zoomIn()
        +zoomOut()
        +fitToView()
        +computeForceDirectedLayout()
        +nodeClicked() Signal
        +edgeClicked() Signal
    }
    
    class InferenceWorker {
        -QString text_
        -QString modelPath_
        -float threshold_
        -InferenceEngine* engine_
        +setText()
        +setModelPath()
        +setThreshold()
        +run()
        +progressChanged() Signal
        +resultReady() Signal
        +errorOccurred() Signal
    }
    
    class InferenceEngine {
        -Ort::Env* env_
        -Ort::Session* session_
        -Tokenizer* tokenizer_
        -int maxSeqLength_
        -float threshold_
        -QMap~int,QString~ id2predicate_
        +loadModel() bool
        +isModelLoaded() bool
        +setThreshold()
        +infer() QList~Triple~
        +inferLongText() QList~Triple~
        +unload()
        -runInference() QList~Triple~
        -loadTokenizer() bool
        -loadMetadata() MetadataLoadResult
    }
    
    class BatchProcessor {
        -QStringList files_
        -QString jsonContentField_
        -QString csvContentColumn_
        -QString csvEncoding_
        -QString modelPath_
        -float threshold_
        -bool stopRequested_
        +setFiles()
        +setJsonContentField()
        +setCsvContentColumn()
        +setThreshold()
        +stop()
        +run()
        +progressChanged() Signal
        +fileProcessed() Signal
        +batchFinished() Signal
        -processJsonFile()
        -processCsvFile()
        -extractFromText()
    }
    
    class DatabaseManager {
        -QSqlDatabase db_
        -QString databasePath_
        +instance() DatabaseManager
        +initialize() bool
        +close()
        +insertExtractionRecord() qint64
        +deleteExtractionRecord() bool
        +getAllExtractionRecords() QList~ExtractionRecord~
        +searchExtractionRecords() QList~ExtractionRecord~
        +getRecordCount() int
        +getEntityCount() int
        +getRelationCount() int
        +exportToJson() bool
        +exportToCsv() bool
        +databaseChanged() Signal
    }
    
    class Entity {
        +int id
        +QString name
        +EntityType type
        +int startPos
        +int endPos
        +float confidence
        +typeToString() QString
        +typeToColor() QColor
    }
    
    class Triple {
        +Entity subject
        +Entity object
        +QString relation
        +RelationType relationType
        +float confidence
        +relationTypeToString() QString
    }
    
    class ExtractionRecord {
        +qint64 id
        +QString content
        +QDateTime createdAt
        +int processTimeMs
        +float avgConfidence
        +int entityCount
        +int relationCount
        +QList~Triple~ triples
    }
    
    class ConfigManager {
        -QSettings settings_
        +instance() ConfigManager
        +initialize()
        +getModelPath() QString
        +setModelPath()
        +getThreshold() float
        +setThreshold()
        +autoDetectModelPath() QString
    }
    
    MainWindow --> ExtractionPanel
    MainWindow --> HistoryPanel
    MainWindow --> GraphWidget
    MainWindow --> InferenceWorker
    MainWindow --> BatchProcessor
    MainWindow --> DatabaseManager
    MainWindow --> ConfigManager
    
    InferenceWorker --> InferenceEngine
    BatchProcessor --> InferenceEngine
    
    DatabaseManager --> ExtractionRecord
    DatabaseManager --> Triple
    DatabaseManager --> Entity
    
    GraphWidget --> Triple
    GraphWidget --> Entity
```

**类图说明：**
- **MainWindow**：主窗口类，协调各模块交互，管理UI组件
- **ExtractionPanel**：抽取面板，处理文本输入和抽取触发
- **HistoryPanel**：历史面板，管理历史记录的展示和操作
- **GraphWidget**：图谱组件，负责知识图谱的渲染和交互
- **InferenceWorker**：推理工作线程，封装后台推理流程
- **InferenceEngine**：推理引擎核心，执行ONNX模型推理
- **BatchProcessor**：批量处理器，支持多文件批量处理
- **DatabaseManager**：数据库管理单例，封装所有数据库操作
- **Entity/Triple/ExtractionRecord**：数据模型结构体
- **ConfigManager**：配置管理单例，管理应用配置

### 5.2.2 知识抽取模块时序图

```mermaid
sequenceDiagram
    actor User
    participant UI as MainWindow
    participant EP as ExtractionPanel
    participant IW as InferenceWorker
    participant IE as InferenceEngine
    participant DB as DatabaseManager
    participant RT as ResultTreeWidget
    participant GW as GraphWidget
    
    User->>EP: 输入文本
    EP->>EP: 文本长度检查
    User->>UI: 点击"抽取"按钮
    UI->>UI: onExtractClicked()
    UI->>UI: 显示进度条
    UI->>IW: 创建并启动线程
    activate IW
    
    IW->>IE: loadModel(modelPath)
    activate IE
    IE->>IE: 加载ONNX模型
    IE->>IE: 加载Tokenizer配置
    IE->>IE: 加载Metadata参数
    IE-->>IW: 返回加载状态
    deactivate IE
    
    IW->>IE: infer(text)
    activate IE
    IE->>IE: 文本预处理(小写/trim)
    IE->>IE: Tokenizer编码(input_ids/attention_mask)
    IE->>IE: ONNX Runtime推理
    IE->>IE: 解析输出张量
    IE->>IE: Token空格清理
    IE->>IE: 组装Triple列表
    IE-->>IW: 返回三元组列表
    deactivate IE
    
    IW->>UI: resultReady信号
    deactivate IW
    
    UI->>DB: insertExtractionRecord
    activate DB
    DB->>DB: 插入extraction_records
    DB->>DB: 插入entities
    DB->>DB: 插入relations
    DB-->>UI: 返回记录ID
    deactivate DB
    
    UI->>RT: setResults(三元组列表)
    UI->>UI: 转换为GraphNode/GraphEdge
    UI->>GW: setGraphData(节点, 边)
    activate GW
    GW->>GW: 计算力导向布局
    GW-->>UI: 渲染完成
    deactivate GW
    
    UI-->>User: 显示知识图谱和结果
```

**时序说明：**
1. 用户在ExtractionPanel输入文本并点击抽取
2. MainWindow创建InferenceWorker线程
3. InferenceWorker加载InferenceEngine模型（懒加载）
4. 执行推理：文本预处理→Tokenizer编码→ONNX推理→结果解析
5. 返回结果后保存到DatabaseManager（三张表）
6. 更新ResultTreeWidget和GraphWidget显示

### 5.2.3 批量处理模块时序图

```mermaid
sequenceDiagram
    actor User
    participant BP as BatchProcessor
    participant IE as InferenceEngine
    participant DB as DatabaseManager
    
    User->>BP: 选择文件并启动
    activate BP
    
    BP->>BP: scanFiles()扫描统计记录数
    BP->>IE: 初始化推理引擎
    
    loop 每个文件
        BP->>BP: 解析文件(JSON/CSV)
        BP->>BP: 提取文本列表
        
        alt 多线程模式
            BP->>BP: QtConcurrent并行处理
            par 线程1处理
                BP->>IE: infer(text)
                IE-->>BP: 返回三元组
            and 线程2处理
                BP->>IE: infer(text)
                IE-->>BP: 返回三元组
            end
        else 单线程模式
            loop 每条文本
                BP->>IE: infer(text)
                IE-->>BP: 返回三元组
            end
        end
        
        BP->>BP: 更新进度
    end
    
    BP->>BP: 智能去重(相似度算法)
    BP->>DB: 批量插入数据库
    activate DB
    DB->>DB: 事务开始
    DB->>DB: 批量插入记录
    DB->>DB: 事务提交
    DB-->>BP: 完成
    deactivate DB
    
    BP-->>User: 显示处理报告
    deactivate BP
```

**时序说明：**
1. 用户选择多个文件启动批量处理
2. 扫描阶段统计总记录数用于进度显示
3. 根据配置选择串行或并行处理模式
4. 多线程模式使用QtConcurrent限制2线程
5. 处理完成后执行智能去重
6. 使用数据库事务批量保存结果

### 5.2.4 数据库操作类图

```mermaid
classDiagram
    direction TB
    
    class DatabaseManager {
        -QSqlDatabase db_
        -QString databasePath_
        -bool checkMainThread()
        +instance() DatabaseManager&
        +initialize(databasePath) bool
        +close()
        +insertExtractionRecord(record) qint64
        +updateExtractionRecord(record) bool
        +deleteExtractionRecord(id) bool
        +deleteMultipleRecords(ids) bool
        +getExtractionRecord(id) ExtractionRecord
        +getAllExtractionRecords(limit) QList~ExtractionRecord~
        +searchExtractionRecords(keyword, startTime, endTime, entityType, limit) QList~ExtractionRecord~
        +getRecordCount() int
        +getEntityCount() int
        +getRelationCount() int
        +getAverageConfidence() float
        +insertExtractionRecords(records) bool
        +exportToJson(filePath, ids) bool
        +exportToCsv(filePath, ids) bool
        +databaseChanged() Signal
        +errorOccurred(message) Signal
        -createTables() bool
        -createExtractionRecordsTable() bool
        -createEntitiesTable() bool
        -createRelationsTable() bool
        -saveTriples(recordId, triples)
        -loadTriples(recordId) QList~Triple~
        -classifyDatabaseError(errorMessage) DatabaseErrorType
        -retryInsertExtractionRecords(records, maxRetries) bool
        -executeWithRetry(operation, maxRetries, operationName) bool
    }
    
    class ExtractionRecord {
        +qint64 id
        +QString content
        +QDateTime createdAt
        +int processTimeMs
        +float avgConfidence
        +int entityCount
        +int relationCount
        +QList~Triple~ triples
        +ExtractionRecord()
        +ExtractionRecord(text, results, processTime)
    }
    
    class Entity {
        +int id
        +QString name
        +EntityType type
        +int startPos
        +int endPos
        +float confidence
        +Entity()
        +Entity(name, type, confidence)
        +typeToString() QString
        +typeToString(type) QString
        +stringToType(str) EntityType
        +typeToColor() QColor
    }
    
    class Triple {
        +Entity subject
        +Entity object
        +QString relation
        +RelationType relationType
        +float confidence
        +Triple()
        +Triple(subj, obj, rel, conf)
        +relationTypeToString() QString
        +relationTypeToString(type) QString
        +stringToRelationType(str) RelationType
    }
    
    class DatabaseErrorType {
        <<enumeration>>
        Unknown
        ConnectionError
        ConstraintError
        TimeoutError
        DiskFullError
        PermissionError
        SchemaError
    }
    
    class EntityType {
        <<enumeration>>
        Component = 0
        Fault = 1
        Tool = 2
        Composition = 3
    }
    
    class RelationType {
        <<enumeration>>
        ComponentFault = 0
        PerformanceFault = 1
        DetectionTool = 2
        CompositionRel = 3
    }
    
    DatabaseManager --> ExtractionRecord
    DatabaseManager --> Triple
    DatabaseManager --> Entity
    DatabaseManager --> DatabaseErrorType
    Triple --> Entity
    Entity --> EntityType
    Triple --> RelationType
```

**类图说明：**
- **DatabaseManager**：单例模式，封装所有数据库操作，使用QSqlDatabase连接SQLite
- **ExtractionRecord**：抽取记录数据结构，包含元数据和三元组列表
- **Entity**：实体数据结构，包括名称、类型、位置、置信度
- **Triple**：三元组数据结构，包含主体、客体、关系、置信度
- **DatabaseErrorType**：数据库错误类型枚举，用于错误分类处理
- **EntityType/RelationType**：实体和关系类型枚举，与代码中一致

### 5.2.5 知识图谱渲染类图

```mermaid
classDiagram
    direction TB
    
    class GraphWidget {
        -QGraphicsScene* scene_
        -QHash~QString,GraphNode*~ nodes_
        -QList~GraphEdge*~ edges_
        -QHash~QString,GraphNodeItem*~ nodeItems_
        -QHash~QString,QList~GraphEdge*~~ nodeEdges_
        -bool dragging_
        -QPoint lastMousePos_
        -qreal scaleFactor_
        +GraphWidget(parent)
        +setGraphData(nodes, edges)
        +clearGraph()
        +highlightNode(nodeId)
        +highlightPath(sourceId, targetId)
        +zoomIn()
        +zoomOut()
        +resetZoom()
        +fitToView()
        +handleNodePositionChanged(nodeId, newPos)
        +nodeClicked() Signal
        +edgeClicked() Signal
        -setupScene()
        -createNode(data)
        -createEdge(data)
        -computeForceDirectedLayout(repulsionK, attractionK, damping, iterations)
        -updateConnectedEdges(nodeId)
        -calculateOptimalRadius(nodeCount)
    }
    
    class GraphNode {
        +QString id
        +QString name
        +EntityType type
        +QColor color
        +qreal x
        +qreal y
        +GraphNode()
        +GraphNode(entity, x, y)
    }
    
    class GraphEdge {
        +QString source
        +QString target
        +QString relation
        +float confidence
        +GraphEdge()
        +GraphEdge(src, tgt, rel, conf)
    }
    
    class GraphNodeItem {
        -QString nodeId_
        -GraphWidget* graphWidget_
        +GraphNodeItem(nodeId, graphWidget, x, y, width, height)
        +nodeId() QString
        +itemChange(change, value) QVariant
    }
    
    class QGraphicsView {
        <<Qt基类>>
    }
    
    class QGraphicsEllipseItem {
        <<Qt基类>>
    }
    
    QGraphicsView <|-- GraphWidget
    QGraphicsEllipseItem <|-- GraphNodeItem
    GraphWidget --> GraphNode
    GraphWidget --> GraphEdge
    GraphWidget --> GraphNodeItem
```

**类图说明：**
- **GraphWidget**：继承QGraphicsView，负责知识图谱的渲染和交互
- **GraphNode**：图谱节点数据结构，包含位置、颜色、类型信息
- **GraphEdge**：图谱边数据结构，包含源节点、目标节点、关系信息
- **GraphNodeItem**：继承QGraphicsEllipseItem，自定义节点图形项，支持拖拽
- 使用力导向布局算法自动计算节点位置

### 5.2.6 模块依赖关系图

```mermaid
flowchart TB
    subgraph UI[表示层]
        MW[MainWindow]
        EP[ExtractionPanel]
        HP[HistoryPanel]
        GW[GraphWidget]
        RT[ResultTreeWidget]
        SD[SettingsDialog]
        BPD[BatchProcessDialog]
    end
    
    subgraph Logic[业务逻辑层]
        IW[InferenceWorker]
        BP[BatchProcessor]
    end
    
    subgraph Engine[核心引擎层]
        IE[InferenceEngine]
        TK[Tokenizer]
    end
    
    subgraph Data[数据层]
        DB[DatabaseManager]
        CM[ConfigManager]
        LG[Logger]
    end
    
    subgraph Model[数据模型]
        EN[Entity]
        TR[Triple]
        ER[ExtractionRecord]
    end
    
    subgraph External[外部依赖]
        OR[ONNX Runtime]
        QS[Qt SQL]
        QG[Qt Graphics]
    end
    
    MW --> EP
    MW --> HP
    MW --> GW
    MW --> RT
    MW --> IW
    MW --> BP
    MW --> DB
    MW --> CM
    
    EP --> MW
    HP --> DB
    GW --> IE
    RT --> TR
    
    IW --> IE
    IW --> DB
    BP --> IE
    BP --> DB
    
    IE --> TK
    IE --> OR
    IE --> EN
    IE --> TR
    
    DB --> QS
    DB --> ER
    DB --> EN
    DB --> TR
    
    CM --> LG
    
    GW --> QG
    
    style External fill:#f9f,stroke:#333
```

**依赖说明：**
- **UI层**：MainWindow协调各UI组件，依赖业务逻辑层和数据层
- **业务逻辑层**：InferenceWorker和BatchProcessor依赖InferenceEngine执行推理，依赖DatabaseManager保存结果
- **核心引擎层**：InferenceEngine封装ONNX Runtime和Tokenizer，处理核心推理逻辑
- **数据层**：DatabaseManager使用Qt SQL操作SQLite，ConfigManager管理配置，Logger记录日志
- **数据模型**：Entity、Triple、ExtractionRecord在各层间传递数据
- **外部依赖**：ONNX Runtime提供推理能力，Qt SQL提供数据库访问，Qt Graphics提供图形渲染

### 5.2.7 核心模块实现说明

**知识抽取模块实现：**
- **InferenceEngine**：核心推理类，封装ONNX Runtime推理流程
  - `loadModel()`：加载ONNX模型、Tokenizer配置、Metadata参数
  - `infer()`：单条文本推理，返回三元组列表
  - `inferLongText()`：长文本分块处理，智能去重
  - `runInference()`：执行实际推理，解析模型输出张量
  
- **InferenceWorker**：QThread子类，后台执行推理避免UI阻塞
  - 接收文本和模型路径配置
  - 异步执行推理，通过信号返回结果
  - 支持进度反馈和错误处理

- **BatchProcessor**：批量处理线程，支持多文件并行处理
  - 扫描文件统计记录数
  - 支持JSON/CSV格式解析
  - 多线程并行处理（限制2线程）
  - 智能去重和批量保存

**知识图谱展示模块实现：**
- **GraphWidget**：知识图谱可视化组件
  - 接收GraphNode和GraphEdge数据
  - 执行力导向布局算法
  - 渲染节点、边、标签
  - 处理鼠标交互（缩放、拖拽、点击）

- **GraphNode/GraphEdge**：图谱数据结构
  - 从Triple转换生成
  - 去重处理避免重复节点

- **GraphNodeItem**：自定义图形项
  - 继承QGraphicsEllipseItem
  - 支持拖拽移动
  - 实时更新关联边位置

**数据库管理模块实现：**
- **DatabaseManager**：单例模式数据库访问类
  - 初始化SQLite数据库连接
  - 创建三张核心表（extraction_records、entities、relations）
  - 提供CRUD操作和批量操作
  - 支持导出JSON/CSV
  - 错误处理和重试机制

**配置管理模块实现：**
- **ConfigManager**：单例模式配置管理类
  - 使用QSettings持久化存储
  - 管理模型路径、置信度阈值、字段映射等配置
  - 支持自动检测模型路径
  - 配置变更信号通知

## 5.3 UI界面截图

以下为系统主要界面截图示例（实际报告中插入真实截图）：

- 图5-1 登录界面

- 图5-2 知识抽取界面

- 图5-3 图谱浏览界面

- 图5-4 知识检索界面

- 图5-5 审核管理界面

演示视频（时长约3分钟）单独提交。

