SourceURL:file:///home/arch/Downloads/软件工程大作业.docx

# 第4章 系统设计

## 4.1 系统总体设计

### 4.1.1 系统架构设计

系统采用分层架构，划分为表示层、业务逻辑层、核心引擎层和数据存储层。

```mermaid
flowchart TB
    subgraph 表示层[表示层 - Qt6 Widgets]
        UI1[主界面<br/>MainWindow]
        UI2[抽取面板<br/>ExtractionPanel]
        UI3[历史面板<br/>HistoryPanel]
        UI4[图谱组件<br/>GraphWidget]
        UI5[结果树<br/>ResultTreeWidget]
    end
    
    subgraph 业务逻辑层[业务逻辑层]
        BL1[知识抽取服务<br/>InferenceWorker]
        BL2[批量处理服务<br/>BatchProcessor]
        BL3[图谱管理服务<br/>GraphWidget逻辑]
        BL4[检索服务<br/>HistoryPanel逻辑]
    end
    
    subgraph 核心引擎层[核心引擎层]
        EN1[ONNX Runtime<br/>推理引擎]
        EN2[Tokenizer<br/>分词器]
        EN3[力导向布局<br/>算法引擎]
    end
    
    subgraph 数据存储层[数据存储层]
        DB1[(SQLite<br/>extraction_records)]
        DB2[(SQLite<br/>entities)]
        DB3[(SQLite<br/>relations)]
        FS[文件系统<br/>模型/配置]
    end
    
    UI1 --> BL1
    UI1 --> BL2
    UI1 --> BL3
    UI1 --> BL4
    
    BL1 --> EN1
    BL1 --> EN2
    BL2 --> EN1
    BL2 --> EN2
    BL3 --> EN3
    
    BL1 --> DB1
    BL1 --> DB2
    BL1 --> DB3
    BL2 --> DB1
    BL2 --> DB2
    BL2 --> DB3
    BL4 --> DB1
    
    EN1 --> FS
    EN2 --> FS
```

**架构说明：**
- 表示层：Qt框架实现，提供文本上传、图谱展示、检索面板、管理控制台。包括MainWindow主窗口、ExtractionPanel文本输入面板、HistoryPanel历史记录面板、GraphWidget知识图谱组件、ResultTreeWidget结果树组件。
- 业务逻辑层：包括知识抽取服务（InferenceWorker推理工作线程）、批量处理服务（BatchProcessor批量处理器）、图谱管理服务（GraphWidget布局计算和交互逻辑）、检索服务（HistoryPanel搜索筛选逻辑）。
- 核心引擎层：ONNX Runtime推理引擎执行深度学习模型推理，Tokenizer分词器基于tokenizers-cpp实现文本编码，力导向布局算法引擎计算知识图谱节点位置。
- 数据存储层：SQLite数据库存储extraction_records抽取记录表、entities实体表、relations关系表，文件系统存储ONNX模型文件、Tokenizer配置和Metadata元数据。

### 4.1.2 系统功能结构图

系统功能分为三大模块：知识抽取模块、知识管理模块、系统管理模块。

```mermaid
flowchart TB
    subgraph OptiKG[OptiKG工业知识抽取系统]
        direction TB
        
        subgraph M1[知识抽取模块]
            M1_1[文本输入]
            M1_2[批量处理]
            M1_3[模型推理]
            M1_4[结果解析]
        end
        
        subgraph M2[知识管理模块]
            M2_1[历史记录]
            M2_2[知识检索]
            M2_3[数据导出]
            M2_4[统计分析]
        end
        
        subgraph M3[系统管理模块]
            M3_1[系统配置]
            M3_2[模型管理]
            M3_3[日志管理]
        end
    end
    
    M1 --> M2
    M3 --> M1
    M3 --> M2
```

**功能模块说明：**
- 知识抽取模块：包含文本输入（支持直接输入和文件上传）、批量处理（支持CSV/JSON多文件处理）、模型推理（ONNX Runtime深度学习推理）、结果解析（实体识别和关系抽取）。
- 知识管理模块：包含历史记录（抽取结果的持久化存储）、知识检索（关键词搜索和筛选）、数据导出（JSON/CSV格式导出）、统计分析（记录数量、实体关系统计）。
- 系统管理模块：包含系统配置（阈值、字段映射等参数配置）、模型管理（ONNX模型热加载和切换）、日志管理（操作日志和错误追踪）。

## 4.2 数据库设计

### 4.2.1 实体分析

核心实体包括：抽取记录、实体、关系。实体间关系：抽取记录包含多个实体和多个关系，实体通过关系相互关联。

### 4.2.2 E-R图

```mermaid
erDiagram
    EXTRACTION_RECORD ||--o{ ENTITY : contains
    EXTRACTION_RECORD ||--o{ RELATION : contains
    ENTITY ||--o{ RELATION : subject_of
    ENTITY ||--o{ RELATION : object_of
    
    EXTRACTION_RECORD {
        integer id PK
        text content
        datetime created_at
        integer process_time_ms
        real avg_confidence
        integer entity_count
        integer relation_count
        text raw_text
        text source_file
    }
    
    ENTITY {
        integer id PK
        integer record_id FK
        text name
        integer type
        integer start_pos
        integer end_pos
        real confidence
    }
    
    RELATION {
        integer id PK
        integer record_id FK
        integer subject_id FK
        integer object_id FK
        text relation
        integer relation_type
        real confidence
    }
```

**实体关系说明：**
- 抽取记录(ExtractionRecord)：一次知识抽取的完整记录，包含原始文本、处理时间、统计信息。
- 实体(Entity)：从文本中识别的命名实体，包括部件、故障、工具、组成四种类型。
- 关系(Relation)：实体之间的关联关系，通过subject_id和object_id关联到实体表。
- 一个抽取记录包含多个实体（一对多），一个抽取记录包含多个关系（一对多），一个实体可以作为多个关系的主体或客体。

### 4.2.3 逻辑结构设计

主要表结构如下：

**表4-1 抽取记录表(extraction_records)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 记录ID |
| content | TEXT | NOT NULL | 原始文本内容 |
| created_at | DATETIME | NOT NULL | 创建时间 |
| process_time_ms | INTEGER | NOT NULL | 处理耗时(毫秒) |
| avg_confidence | REAL | NOT NULL | 平均置信度 |
| entity_count | INTEGER | NOT NULL | 实体数量 |
| relation_count | INTEGER | NOT NULL | 关系数量 |
| raw_text | TEXT | | 原始文本备份 |
| source_file | TEXT | | 来源文件路径 |

**索引：** idx_records_created_at ON created_at, idx_records_confidence ON avg_confidence

**表4-2 实体表(entities)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 实体ID |
| record_id | INTEGER | FOREIGN KEY | 所属记录ID |
| name | TEXT | NOT NULL | 实体名称 |
| type | INTEGER | NOT NULL | 实体类型(0-3) |
| start_pos | INTEGER | | 开始位置 |
| end_pos | INTEGER | | 结束位置 |
| confidence | REAL | NOT NULL | 置信度 |

**索引：** idx_entities_record_id ON record_id, idx_entities_type ON type

**表4-3 关系表(relations)**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 关系ID |
| record_id | INTEGER | FOREIGN KEY | 所属记录ID |
| subject_id | INTEGER | FOREIGN KEY | 主体实体ID |
| object_id | INTEGER | FOREIGN KEY | 客体实体ID |
| relation | TEXT | NOT NULL | 关系名称 |
| relation_type | INTEGER | NOT NULL | 关系类型(0-3) |
| confidence | REAL | NOT NULL | 置信度 |

**索引：** idx_relations_record_id ON record_id, idx_relations_relation ON relation

**外键约束：** record_id引用extraction_records(id) ON DELETE CASCADE, subject_id和object_id引用entities(id) ON DELETE CASCADE

## 4.3 UI设计

系统主界面采用三栏布局：

```mermaid
flowchart TB
    subgraph MainWindow[主窗口 MainWindow]
        direction TB
        
        subgraph MenuBar[菜单栏]
            M1[文件菜单]
            M2[编辑菜单]
            M3[视图菜单]
            M4[工具菜单]
            M5[帮助菜单]
        end
        
        subgraph ToolBar[工具栏]
            T1[抽取按钮]
            T2[清空按钮]
            T3[放大/缩小]
            T4[适应视图]
            T5[设置按钮]
            T6[批量处理]
        end
        
        subgraph CentralWidget[中心部件 - 分割器布局]
            direction TB
            
            subgraph TopArea[上部主区域 - 水平分割]
                direction LR
                
                subgraph LeftPanel[左侧面板]
                    direction TB
                    LP1[抽取面板<br/>ExtractionPanel<br/>文本输入区]
                    LP2[历史记录面板<br/>HistoryPanel<br/>表格显示]
                end
                
                subgraph RightPanel[右侧面板]
                    direction TB
                    RP1[知识图谱<br/>GraphWidget<br/>可视化区域]
                    RP2[结果树<br/>ResultTreeWidget<br/>列表显示]
                end
            end
            
            subgraph StatusBar[底部状态栏]
                SB1[状态标签]
                SB2[模式显示]
                SB3[阈值显示]
                SB4[计数显示]
                SB5[进度条]
            end
        end
    end
```

- 左侧导航栏：知识抽取、图谱浏览、知识检索、审核管理、系统设置；
- 中间内容区：根据功能展示相应内容（文件上传区、图谱展示区、检索结果列表等）；
- 底部状态栏：显示登录用户、模型版本、系统状态。

相关界面元素包括：文本输入框、抽取按钮、图谱视图、结果列表、历史表格、状态栏等。

## 4.4 网络设计

OptiKG定位为完全离线运行的系统，不依赖互联网连接。部署模式为单机部署，所有组件运行在同一台工业计算机上，不开放任何网络端口。如需与其他系统交换数据，通过离线文件（CSV/JSON）导入导出。后续引入openClaw Agent时，可部署在同一内网，通过本地HTTP服务交互。

```mermaid
flowchart TB
    subgraph Deployment[单机部署架构]
        subgraph LocalMachine[本地工业计算机]
            APP[OptiKG应用程序]
            DB[(SQLite数据库)]
            FS[模型文件系统]
            
            APP <---> DB
            APP <---> FS
        end
        
        subgraph External[外部交互]
            Import[导入<br/>CSV/JSON/TXT]
            Export[导出<br/>CSV/JSON]
        end
        
        Import --> APP
        APP --> Export
    end
```

## 4.5 数据量分析

以中型企业为例：

- 用户数量：约20人；
- 文档数量：年新增约2000份，每份约300字；
- 三元组数量：每份约3个，年新增约6000条；
- 存储需求：文档存储约2MB/年，三元组存储约4MB/三年，模型文件约200MB，总计<250MB。普通工业计算机硬盘完全满足。

## 4.6 核心业务流程活动图

### 4.6.1 知识抽取模块活动图

```mermaid
flowchart TD
    Start([开始]) --> Input[用户输入文本]
    Input --> CheckLength{文本长度<br/>检查}
    CheckLength -- >5000字符 --> SplitText[长文本分块<br/>500字符/块<br/>重叠100字符]
    CheckLength -- ≤5000字符 --> Preprocess[文本预处理]
    SplitText --> Preprocess
    
    Preprocess --> LoadModel{模型<br/>已加载?}
    LoadModel -- 否 --> InitONNX[初始化ONNX Runtime环境]
    InitONNX --> LoadTokenizer[加载Tokenizer配置]
    LoadTokenizer --> LoadMetadata[加载Metadata<br/>max_len/threshold/id2predicate]
    LoadMetadata --> SetModelFlag[设置模型已加载标志]
    SetModelFlag --> Encode
    LoadModel -- 是 --> Encode[Tokenizer编码<br/>生成input_ids和attention_mask]
    
    Encode --> RunInference[ONNX Runtime推理执行]
    RunInference --> ParseOutput[解析模型输出张量<br/>entLogits/relHLogits/relTLogits]
    
    ParseOutput --> ExtractEntity[提取实体Span<br/>start/end/type/confidence]
    ExtractEntity --> ExtractRelation[提取关系Span<br/>计算置信度]
    
    ExtractRelation --> Postprocess[后处理<br/>Token空格清理<br/>置信度过滤]
    Postprocess --> AssembleTriple[组装三元组<br/>subject-object-relation]
    
    AssembleTriple --> SaveDB[保存到数据库<br/>extraction_records<br/>entities<br/>relations]
    SaveDB --> UpdateUI[更新UI显示<br/>结果树+知识图谱]
    UpdateUI --> ShowResult[显示处理结果<br/>耗时/置信度/三元组数量]
    ShowResult --> End([结束])
    
    CheckLength -- 空文本 --> Error1[提示:请输入文本]
    Error1 --> Input
    LoadTokenizer -- 失败 --> Error2[提示:模型加载失败]
    Error2 --> End
    RunInference -- 异常 --> Error3[提示:推理失败]
    Error3 --> End
```

**主要活动说明：**
- 用户上传文本→文本长度检查（超过5000字符则分块）→文本预处理（转小写、trim、JSON解析）→模型加载（ONNX+Tokenizer+Metadata）→Tokenizer编码（生成input_ids和attention_mask）→ONNX模型推理→解码抽取结果（解析entLogits/relHLogits/relTLogits）→后处理（Token空格清理、置信度过滤）→组装三元组→写入数据库（extraction_records/entities/relations三张表）→生成处理报告→更新UI显示→提示完成。

### 4.6.2 批量处理模块活动图

```mermaid
flowchart TD
    Start([开始]) --> SelectFiles[用户选择<br/>CSV/JSON文件]
    SelectFiles --> ScanFiles[扫描文件<br/>统计总记录数]
    ScanFiles --> ConfigField[配置字段映射<br/>JSON字段名/CSV列名]
    ConfigField --> InitEngine[初始化推理引擎<br/>加载ONNX模型]
    
    InitEngine --> CheckFileType{文件类型}
    CheckFileType -- JSON --> ParseJSON[解析JSON文件<br/>支持标准JSON和JSON Lines]
    CheckFileType -- CSV --> ParseCSV[解析CSV文件<br/>自动检测编码UTF-8/GBK]
    
    ParseJSON --> ExtractText[提取文本字段内容]
    ParseCSV --> ExtractText
    
    ExtractText --> CheckMultiThread{启用<br/>多线程?}
    CheckMultiThread -- 是 --> ParallelProcess[并行处理<br/>QtConcurrent<br/>限制2线程]
    CheckMultiThread -- 否 --> SerialProcess[串行处理]
    
    ParallelProcess --> ProcessEach[处理每条文本]
    SerialProcess --> ProcessEach
    
    subgraph ProcessBlock[单条文本处理]
        P1[Tokenizer编码]
        P2[ONNX推理]
        P3[解析结果]
        P4[组装三元组]
        P1 --> P2 --> P3 --> P4
    end
    
    ProcessEach --> ProcessBlock
    ProcessBlock --> UpdateProgress[更新进度条<br/>当前记录数/总记录数]
    UpdateProgress --> MoreRecords{还有更多<br/>记录?}
    MoreRecords -- 是 --> ProcessEach
    MoreRecords -- 否 --> Deduplicate[智能去重<br/>基于相似度算法]
    
    Deduplicate --> SaveResults[保存所有结果<br/>批量插入数据库]
    SaveResults --> GenerateReport[生成处理报告<br/>成功数/失败数/总耗时]
    GenerateReport --> ShowResult[显示批量处理结果]
    ShowResult --> End([结束])
    
    InitEngine -- 加载失败 --> Error1[提示:模型加载失败<br/>跳过推理]
    Error1 --> End
    ParseJSON -- 解析失败 --> Error2[提示:JSON格式错误]
    Error2 --> End
    ParseCSV -- 列不存在 --> Error3[提示:指定的列不存在]
    Error3 --> End
```

**主要活动说明：**
- 用户选择文件→扫描文件统计记录数→配置字段映射→初始化推理引擎→解析文件（JSON/CSV）→提取文本内容→多线程并行处理→每条文本执行推理→更新进度→智能去重→批量插入数据库→生成处理报告→显示结果。

### 4.6.3 知识管理模块活动图

```mermaid
flowchart TD
    Start([开始]) --> LoadHistory[加载历史记录<br/>从extraction_records表]
    LoadHistory --> DisplayTable[以表格形式展示<br/>ID/内容/时间/置信度/数量]
    
    DisplayTable --> UserAction{用户操作}
    
    UserAction -- 搜索 --> InputKeyword[输入关键词]
    InputKeyword --> FilterType[选择实体类型筛选]
    FilterType --> FilterTime[选择时间范围]
    FilterTime --> ExecuteSearch[执行搜索查询<br/>SQL LIKE + 条件组合]
    ExecuteSearch --> UpdateTable[更新表格显示]
    UpdateTable --> UserAction
    
    UserAction -- 查看详情 --> SelectRecord[选择记录]
    SelectRecord --> LoadTriples[加载三元组<br/>从entities和relations表]
    LoadTriples --> ShowDetail[显示详情对话框<br/>原文+三元组列表]
    ShowDetail --> UserAction
    
    UserAction -- 删除记录 --> SelectDelete[选择要删除的记录]
    SelectDelete --> ConfirmDelete{确认删除?}
    ConfirmDelete -- 是 --> DeleteDB[从数据库删除<br/>级联删除关联实体和关系]
    ConfirmDelete -- 否 --> UserAction
    DeleteDB --> RefreshTable[刷新表格]
    RefreshTable --> UserAction
    
    UserAction -- 批量删除 --> MultiSelect[多选记录]
    MultiSelect --> ConfirmBatchDelete{确认批量删除?}
    ConfirmBatchDelete -- 是 --> BatchDeleteDB[批量删除记录]
    ConfirmBatchDelete -- 否 --> UserAction
    BatchDeleteDB --> RefreshTable2[刷新表格]
    RefreshTable2 --> UserAction
    
    UserAction -- 导出数据 --> SelectExport[选择导出格式<br/>JSON/CSV]
    SelectExport --> SelectPath[选择保存路径]
    SelectPath --> GenerateFile[生成导出文件]
    GenerateFile --> ShowExportSuccess[提示导出成功]
    ShowExportSuccess --> UserAction
    
    UserAction -- 退出 --> End([结束])
```

**主要活动说明：**
- 加载历史记录→表格展示→用户操作（搜索/查看/删除/导出）→搜索时执行SQL组合查询→查看时加载关联三元组→删除时级联删除关联数据→导出时生成JSON/CSV文件。

### 4.6.4 知识图谱展示模块活动图

```mermaid
flowchart TD
    Start([开始]) --> ReceiveData[接收三元组数据]
    ReceiveData --> ConvertNode[转换为GraphNode<br/>去重实体]
    ConvertNode --> ConvertEdge[转换为GraphEdge<br/>去重关系]
    
    ConvertEdge --> InitLayout[初始化力导向布局参数<br/>repulsionK/attractionK/damping]
    InitLayout --> RandomPosition[随机初始化节点位置]
    
    RandomPosition --> IterationStart[开始迭代计算<br/>默认100次迭代]
    
    subgraph LayoutIteration[单次迭代]
        L1[计算节点间斥力]
        L2[计算边引力]
        L3[更新节点位置]
        L4[应用阻尼系数]
        L1 --> L2 --> L3 --> L4
    end
    
    IterationStart --> LayoutIteration
    LayoutIteration --> CheckConvergence{收敛或<br/>达到最大迭代?}
    CheckConvergence -- 否 --> LayoutIteration
    CheckConvergence -- 是 --> CreateGraphics[创建图形项]
    
    subgraph CreateElements[创建图形元素]
        C1[创建节点椭圆<br/>不同颜色区分类型]
        C2[创建边线条]
        C3[创建关系标签]
        C4[创建图例]
        C1 --> C2 --> C3 --> C4
    end
    
    CreateGraphics --> CreateElements
    CreateElements --> AddToScene[添加到QGraphicsScene]
    AddToScene --> RenderView[渲染到QGraphicsView]
    
    RenderView --> WaitInteraction[等待用户交互]
    
    WaitInteraction --> InteractionType{交互类型}
    
    InteractionType -- 缩放 --> WheelEvent[滚轮事件]
    WheelEvent --> CalculateScale[计算缩放比例<br/>0.8-1.2倍]
    CalculateScale --> ApplyScale[应用缩放变换]
    ApplyScale --> WaitInteraction
    
    InteractionType -- 拖拽节点 --> MousePress[鼠标按下]
    MousePress --> MouseMove[鼠标移动]
    MouseMove --> UpdateNodePos[更新节点位置]
    UpdateNodePos --> UpdateEdgePos[同步更新关联边位置]
    UpdateEdgePos --> MouseRelease[鼠标释放]
    MouseRelease --> WaitInteraction
    
    InteractionType -- 点击节点 --> NodeClick[节点点击事件]
    NodeClick --> HighlightNode[高亮显示节点]
    HighlightNode --> ShowNodeInfo[显示节点信息]
    ShowNodeInfo --> WaitInteraction
    
    InteractionType -- 点击边 --> EdgeClick[边点击事件]
    EdgeClick --> HighlightEdge[高亮显示边]
    HighlightEdge --> ShowRelationInfo[显示关系信息]
    ShowRelationInfo --> WaitInteraction
    
    InteractionType -- 路径追踪 --> SelectPath[选择起点和终点]
    SelectPath --> FindPath[查找最短路径]
    FindPath --> HighlightPath[高亮显示路径]
    HighlightPath --> WaitInteraction
    
    InteractionType -- 重置视图 --> ResetView[重置缩放和位置]
    ResetView --> WaitInteraction
    
    InteractionType -- 适应视图 --> FitView[计算边界框]
    FitView --> AdjustScale[调整缩放比例]
    AdjustScale --> CenterView[居中显示]
    CenterView --> WaitInteraction
    
    InteractionType -- 退出 --> End([结束])
```

**主要活动说明：**
- 接收三元组数据→转换为GraphNode和GraphEdge（去重）→力导向布局计算（Fruchterman-Reingold算法，100次迭代）→创建图形元素（节点/边/标签/图例）→添加到场景渲染→等待用户交互→支持缩放/拖拽/点击高亮/路径追踪等交互操作。

### 4.6.5 历史记录管理模块活动图

```mermaid
flowchart TD
    Start([开始]) --> InitPanel[初始化历史面板]
    InitPanel --> CreateControls[创建控件<br/>搜索框/筛选器/表格/按钮]
    CreateControls --> ConnectSignals[连接信号槽]
    ConnectSignals --> LoadData[加载初始数据<br/>显示最近100条记录]
    
    LoadData --> SetupTable[设置表格列<br/>ID/内容/时间/置信度/实体数/关系数]
    SetupTable --> RenderTable[渲染表格数据]
    RenderTable --> WaitUser[等待用户操作]
    
    WaitUser --> UserAction{用户操作}
    
    UserAction -- 输入搜索词 --> InputSearch[在搜索框输入关键词]
    InputSearch --> Debounce[防抖处理<br/>300ms延迟]
    Debounce --> SearchDB[执行数据库搜索<br/>SQL LIKE查询content字段]
    SearchDB --> FilterResult[过滤结果]
    FilterResult --> UpdateTable[更新表格显示]
    UpdateTable --> WaitUser
    
    UserAction -- 选择实体类型 --> SelectType[在下拉框选择类型]
    SelectType --> CheckSearch{已有<br/>搜索词?}
    CheckSearch -- 是 --> CombinedSearch[组合查询<br/>关键词+类型]
    CheckSearch -- 否 --> TypeFilter[仅按类型过滤]
    CombinedSearch --> UpdateTable2[更新表格]
    TypeFilter --> UpdateTable2
    UpdateTable2 --> WaitUser
    
    UserAction -- 选择时间范围 --> SelectDate[选择开始/结束日期]
    SelectDate --> DateFilter[按时间范围过滤]
    DateFilter --> UpdateTable3[更新表格]
    UpdateTable3 --> WaitUser
    
    UserAction -- 点击单条记录 --> ClickRecord[单击记录行]
    ClickRecord --> EmitClickSignal[发送recordClicked信号]
    EmitClickSignal --> LoadDetail[主窗口加载记录详情]
    LoadDetail --> ShowInUI[在结果树和图谱中显示]
    ShowInUI --> WaitUser
    
    UserAction -- 双击记录 --> DoubleClick[双击记录行]
    DoubleClick --> EmitDoubleSignal[发送recordDoubleClicked信号]
    EmitDoubleSignal --> OpenDetailDialog[打开详情对话框]
    OpenDetailDialog --> WaitUser
    
    UserAction -- 多选记录 --> MultiSelect[按住Ctrl/Cmd多选]
    MultiSelect --> EnableBatchBtn[启用批量删除按钮]
    EnableBatchBtn --> WaitUser
    
    UserAction -- 批量删除 --> ClickBatchDelete[点击批量删除按钮]
    ClickBatchDelete --> ConfirmDialog[弹出确认对话框]
    ConfirmDialog --> UserConfirm{用户确认?}
    UserConfirm -- 否 --> WaitUser
    UserConfirm -- 是 --> DeleteRecords[调用DatabaseManager<br/>deleteMultipleRecords]
    DeleteRecords --> CheckResult{删除成功?}
    CheckResult -- 是 --> ShowSuccess[提示删除成功]
    CheckResult -- 否 --> ShowError[提示删除失败]
    ShowSuccess --> RefreshData[刷新表格数据]
    ShowError --> WaitUser
    RefreshData --> WaitUser
    
    UserAction -- 导出选中 --> ClickExport[点击导出按钮]
    ClickExport --> CheckSelected{有选中记录?}
    CheckSelected -- 否 --> WaitUser
    CheckSelected -- 是 --> EmitExportSignal[发送exportClicked信号]
    EmitExportSignal --> MainExport[主窗口执行导出]
    MainExport --> WaitUser
    
    UserAction -- 清空筛选 --> ClickClear[点击清空筛选按钮]
    ClickClear --> ResetFilters[重置所有筛选条件]
    ResetFilters --> ReloadAll[重新加载全部数据]
    ReloadAll --> WaitUser
    
    UserAction -- 刷新 --> ClickRefresh[点击刷新按钮]
    ClickRefresh --> ReloadData[重新加载数据]
    ReloadData --> WaitUser
    
    UserAction -- 退出面板 --> End([结束])
```

**主要活动说明：**
- 初始化面板→创建控件→加载数据→设置表格→等待用户操作→搜索（防抖300ms）→组合筛选→单击/双击查看→多选批量删除→导出数据→清空筛选→刷新数据。

### 4.6.6 数据导出模块活动图

```mermaid
flowchart TD
    Start([开始]) --> CheckData{有数据<br/>可导出?}
    CheckData -- 否 --> ShowNoData[提示:没有可导出的数据]
    ShowNoData --> End([结束])
    CheckData -- 是 --> SelectFormat{选择格式}
    
    SelectFormat -- JSON --> JSONPath[选择JSON保存路径]
    SelectFormat -- CSV --> CSVPath[选择CSV保存路径]
    
    JSONPath --> BuildJSON[构建JSON文档]
    subgraph JSONBuild[JSON构建过程]
        J1[创建根对象]
        J2[添加版本信息]
        J3[添加导出时间]
        J4[添加三元组数量]
        J5[构建triples数组]
        J6[遍历每个三元组]
        J7[添加subject对象<br/>name/type/confidence]
        J8[添加object对象<br/>name/type/confidence]
        J9[添加relation字段]
        J10[添加confidence字段]
        J1 --> J2 --> J3 --> J4 --> J5 --> J6 --> J7 --> J8 --> J9 --> J10
    end
    BuildJSON --> JSONBuild
    JSONBuild --> WriteJSON[写入JSON文件<br/>Indented格式]
    WriteJSON --> JSONSuccess{写入成功?}
    JSONSuccess -- 是 --> ShowJSONSuccess[提示导出成功]
    JSONSuccess -- 否 --> ShowJSONError[提示写入失败]
    ShowJSONSuccess --> End
    ShowJSONError --> End
    
    CSVPath --> BuildCSV[构建CSV内容]
    subgraph CSVBuild[CSV构建过程]
        C1[写入UTF-8 BOM]
        C2[写入表头行<br/>subject_name/subject_type/relation...]
        C3[遍历每个三元组]
        C4[CSV转义处理<br/>处理逗号/引号/换行]
        C5[写入数据行]
        C1 --> C2 --> C3 --> C4 --> C5
    end
    BuildCSV --> CSVBuild
    CSVBuild --> WriteCSV[写入CSV文件]
    WriteCSV --> CSVSuccess{写入成功?}
    CSVSuccess -- 是 --> ShowCSVSuccess[提示导出成功]
    CSVSuccess -- 否 --> ShowCSVError[提示写入失败]
    ShowCSVSuccess --> End
    ShowCSVError --> End
```

**主要活动说明：**
- 检查数据→选择格式（JSON/CSV）→构建文档→JSON包含版本/时间/数量/triples数组→CSV包含UTF-8 BOM和表头→CSV转义处理特殊字符→写入文件→提示成功/失败。

### 4.6.7 系统配置模块活动图

```mermaid
flowchart TD
    Start([开始]) --> OpenSettings[用户打开设置对话框]
    OpenSettings --> LoadCurrent[加载当前配置]
    
    subgraph ConfigItems[配置项]
        CI1[模型路径]
        CI2[置信度阈值<br/>默认-10.0]
        CI3[JSON内容字段名<br/>默认text]
        CI4[CSV内容列名<br/>默认content]
        CI5[CSV编码<br/>默认UTF-8]
        CI6[窗口几何状态]
    end
    
    LoadCurrent --> ConfigItems
    ConfigItems --> RenderUI[渲染设置界面]
    RenderUI --> WaitUser[等待用户修改]
    
    WaitUser --> UserAction{用户操作}
    
    UserAction -- 修改模型路径 --> ClickBrowse[点击浏览按钮]
    ClickBrowse --> ShowFileDialog[显示文件选择对话框<br/>筛选ONNX文件]
    ShowFileDialog --> SelectModel[选择模型文件]
    SelectModel --> ValidateModel{验证模型<br/>有效性?}
    ValidateModel -- 否 --> ShowModelError[提示:无效的模型文件]
    ShowModelError --> WaitUser
    ValidateModel -- 是 --> UpdatePath[更新路径显示]
    UpdatePath --> WaitUser
    
    UserAction -- 修改阈值 --> AdjustThreshold[调整阈值滑块/输入框]
    AdjustThreshold --> ValidateRange{范围有效-20, 0?}
    ValidateRange -- 否 --> ClampValue[限制在有效范围]
    ValidateRange -- 是 --> UpdateThreshold[更新阈值显示]
    ClampValue --> UpdateThreshold
    UpdateThreshold --> WaitUser
    
    UserAction -- 修改字段映射 --> EditField[输入字段/列名]
    EditField --> UpdateField[更新字段配置]
    UpdateField --> WaitUser
    
    UserAction -- 修改编码 --> SelectEncoding[选择编码]
    SelectEncoding --> UpdateEncoding[更新编码配置]
    UpdateEncoding --> WaitUser
    
    UserAction -- 点击保存 --> ClickSave[点击保存按钮]
    ClickSave --> ValidateAll{验证所有<br/>配置项?}
    ValidateAll -- 否 --> ShowValidationError[显示验证错误]
    ShowValidationError --> WaitUser
    ValidateAll -- 是 --> SaveToStorage[保存到持久化存储]
    
    subgraph SaveProcess[保存过程]
        S1[保存到ConfigManager内存]
        S2[保存到QSettings<br/>Windows注册表/INI文件]
        S3[发射配置变更信号]
    end
    SaveToStorage --> SaveProcess
    SaveProcess --> ShowSaveSuccess[提示保存成功]
    ShowSaveSuccess --> CloseDialog[关闭对话框]
    CloseDialog --> End1([结束])
    
    UserAction -- 点击取消 --> ClickCancel[点击取消按钮]
    ClickCancel --> DiscardChanges[放弃所有修改]
    DiscardChanges --> CloseDialog2[关闭对话框]
    CloseDialog2 --> End2([结束])
    
    UserAction -- 恢复默认 --> ClickReset[点击恢复默认按钮]
    ClickReset --> ConfirmReset{确认恢复?}
    ConfirmReset -- 否 --> WaitUser
    ConfirmReset -- 是 --> ResetDefaults[恢复所有默认配置]
    ResetDefaults --> UpdateUI[更新界面显示]
    UpdateUI --> WaitUser
```

**主要活动说明：**

- 打开设置→加载当前配置→渲染界面→等待修改→修改模型路径（浏览选择并验证）→修改阈值（限制范围[-20,0]）→修改字段映射→修改编码→保存时验证→保存到QSettings→发射变更信号→或取消/恢复默认。

