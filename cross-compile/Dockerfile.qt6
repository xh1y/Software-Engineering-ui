# 方案 1: 使用社区 Qt6 镜像（更快）
# 尝试使用现成的 Qt6 MinGW 镜像

FROM a12e/docker-qt6-windows:6.5-mingw-w64-ltsc2022

# 这个镜像已经包含 Qt6 + MinGW
# 我们只需要添加 protobuf

USER root

# 安装额外工具
RUN apt-get update && apt-get install -y \
    protobuf-compiler \
    libprotobuf-dev \
    wget \
    unzip

WORKDIR /work
